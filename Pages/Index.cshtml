@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<style>
    .hero {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        height: 90vh;
        background: linear-gradient(135deg, #1e3c72, #2a5298);
        color: white;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        text-align: center;
        padding: 2rem;
        border-radius: 20px;
        box-shadow: 0 10px 25px rgba(0,0,0,0.3);
    }

    .hero h1 {
        font-size: 4rem;
        font-weight: 800;
        margin-bottom: 1rem;
        color: #FFD700; /* Gold color for your name */
        text-shadow: 2px 2px 5px rgba(0,0,0,0.5);
    }

    .hero p {
        font-size: 1.5rem;
        font-style: italic;
        color: #e0e0e0;
    }
</style>

<div class="hero">
    <h1>Angela Saroj</h1>
    <p style="font-size: 1.4rem; font-style: italic; color: #ffcccc; max-width: 600px; margin: 0 auto;">
        In a world full of code and chaos, your love is my cleanest syntax.<br />
        With every push and every build, I fall for you deeper—line by line, heartbeat by heartbeat.<br />
        You are the breakpoint where my soul pauses, the logic that completes me.
    </p>
</div>